# ADR-0009：使用本地匿名化 JSONL 记录工具效果

## 状态

Accepted

## 决策

扩展只记录结构化计数和结果，不保存原始提示、参数、输出或路径。数据本地写入 JSONL，通过 `/stats` 聚合。

## 原因

JSONL 追加成本低、损坏隔离简单，也不需要数据库依赖。统计结果表示相关性，不冒充因果证明。

## 备选方案

- 写入会话上下文：拒绝，会污染上下文并增加 token。
- 上传遥测：拒绝，带来隐私和运维成本。
- SQLite：暂不采用，当前数据规模不需要。
