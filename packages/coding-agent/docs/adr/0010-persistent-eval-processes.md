# ADR-0010：使用受控长驻子进程实现 Python/Bun eval

## 状态

Accepted

## 决策

每个项目按语言维护一个子进程，通过 JSON 行协议执行代码。进程按需启动，超时、退出或会话结束时清理。

## 原因

相比每次启动解释器，长驻进程能保留变量并降低连续实验延迟。相比把解释器嵌入 Node，外部进程更容易隔离崩溃和回收。

## 备选方案

- 每次执行新进程：拒绝，不能保留状态且启动慢。
- 直接使用 shell REPL：拒绝，输出协议不稳定。
- 容器沙箱：暂不采用，安装和启动成本过高。
