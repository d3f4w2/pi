# ADR-0012：网页工具采用有界并发和失败隔离

## 状态

Accepted

## 决策

保留无重试网络策略，增加全局并发上限、同请求合并、取消归一化和流清理保护。

## 原因

网页工具是可选加速器。失败应快速、局部且可解释，不能通过重试或失控并发拖慢主任务。

## 备选方案

- 自动指数重试：拒绝，会放大等待时间。
- 无限排队：拒绝，会造成看似卡死。
- 用终端 curl 回退：拒绝，会绕过统一安全边界。
